info:
  name: user-details
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/users/:pk"
  params:
    - name: pk
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
