info:
  name: list-users
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/users/"
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: ""
        disabled: true
      - name: username
        type: text
        value: ""
        disabled: true
      - name: is_verified
        type: file
        value:
          - ""
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
