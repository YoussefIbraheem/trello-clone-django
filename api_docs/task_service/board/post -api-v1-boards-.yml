info:
  name: post /api/v1/boards/
  type: http
  seq: 2
  tags:
    - board

http:
  method: POST
  url: "{{url}}/boards/"
  body:
    type: json
    data: |-
      {
        "name": "",
        "description": "",
        "project_id": 0,
        "columns": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/boards/"
      method: POST
      body:
        type: json
        data: |-
          {
            "name": "",
            "description": "",
            "project_id": 0,
            "columns": []
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
