[tool.basedpyright]
exclude = [
    "**/__pycache__",
    "**/.mypy_cache",
    "**/.pytest_cache"
]

[tool.basedpyright]
venvPath = "."       # Tells it to look in the current project root directory
venv = ".venv"  