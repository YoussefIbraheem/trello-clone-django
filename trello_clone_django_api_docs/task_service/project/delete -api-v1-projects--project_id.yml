info:
  name: delete /api/v1/projects/:project_id
  type: http
  seq: 5
  tags:
    - project

http:
  method: DELETE
  url: "{{url}}/projects/:project_id"
  params:
    - name: project_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/projects/:project_id"
      method: DELETE
      params:
        - name: project_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
